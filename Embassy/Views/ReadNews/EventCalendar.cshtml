@model IEnumerable<EmbassyAPI.Models.ViewModels.News>
@*@using Embassy.Models*@
@functions{
    enum Months
    {
        January = 1, February = 2, March = 3, Aprel = 4, May = 5, June = 6, July = 7, August = 8, September = 9, October = 10, November = 11, December = 12
    }
}


<div class="container-fluid ">
    <div class="row ">
        <div class="col-md-12">
            <h3 style=" margin: 0px 0 20px 0">Event Calendar</h3>
            <hr style="width: 1100px;border-color:red;margin-left:0;margin-top:0" />
            @foreach (var item in Model)
            {
                <button class="rounded btn-secondary" data-toggle="collapse" data-target="#@string.Concat(item.ISDELETED,DateTime.Parse(item.DATEPOSTED.ToString()).Year.ToString(),item.NEWSID)" style="padding:10px;margin-bottom:10px;width:1100px">@DateTime.Parse(item.DATEPOSTED.ToString()).Year.ToString() </button>
                <br />
                <div id="@string.Concat(item.ISDELETED,DateTime.Parse(item.DATEPOSTED.ToString()).Year.ToString(),item.NEWSID)" class="collapse">

                    @*@foreach (var month in Enum.GetValues(typeof(Months)))
                    {*@
                    <button class="rounded btn-danger" data-toggle="collapse" data-target="#@string.Concat(item.ISDELETED,DateTime.Parse(item.DATEPOSTED.ToString()).ToString("MMMM"),item.ISARCHIVE,item.NEWSID)" style="padding:10px;margin-bottom:10px;width:1100px">@DateTime.Parse(item.DATEPOSTED.ToString()).ToString("MMMM")</button>
                    @*if (DateTime.Parse(item.DATEPOSTED.ToString()).ToString("MMMM") == month.ToString())
                    {*@
                    <div id="@string.Concat(item.ISDELETED,DateTime.Parse(item.DATEPOSTED.ToString()).ToString("MMMM"),item.ISARCHIVE,item.NEWSID)" class="collapse">
                        @item.NEWSCONTENT
                    </div>
                    <br />
                    @*}*@
                    @*}*@
                </div>
            }
        </div>
    </div>
</div>
