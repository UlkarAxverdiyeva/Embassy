
@{
    ViewBag.Title = "AddNewsView";
    Layout = "~/Views/Shared/RenderSectionLayout.cshtml";
}

<div class="container" style="margin-top:10%">
    <div class="row">
        <div class="col-md-4 col-md-offset-4 alert alert-success GetInfo">
            <h3 class="text-center">AddNews</h3>
            <form class="addForm">
@*                @Html.ValidationSummary(false, "", new { @class = "text-danger" })
*@                @*<div id="msg"><ul style="color:red;"> Invalid Username Or Password</ul></div>*@
                <div class="form-group">
                    <div class="input-group">
                        <span class="input-group-addon"><i class="glyphicon glyphicon-pencil"></i></span>
                        <input class="form-control" type="text" name="NEWSTITLE" id="NEWSTITLE" required placeholder="NEWSTITLE"  @* oninvalid="this.setCustomValidity('Enter User Name Here')"  onchange="this.setCustomValidity('')"*@/>
                    </div>
                </div>

                <div class="form-group">
                    <div class="input-group">
                        <span class="input-group-addon"><i class="glyphicon glyphicon-pencil"></i></span>
                        <input required class="form-control" type="text" name="NEWSCONTENT" id="NEWSCONTENT" placeholder="NEWSCONTENT" @*oninvalid = "this.setCustomValidity('Xana  doldurulmayıb')" oninput = "setCustomValidity('')"  *@ />
                    </div>
                </div>

            </form>
            <div class="form-group">
                <button class="btn btn-secondary form-control" type="submit" onclick="Add()"><i class="glyphicon glyphicon-log-in"></i> Add</button>
            </div>

        </div>
    </div>
</div>


<script>
    function Add() {
        $.ajax({
            data: $('.addForm').serialize(),
            url: '@Url.Action("AddNewsView", "Admin")',
            type: 'POST',
            success: function (result) {
            if (result == "Fail") {
            //alert("yox");
         //   $("#loginForm")[0].reset();
            swal("Uğursuz!", "", "");
            //setTimeout(function () { location.reload(); }, 2000);
            }
            else {
            //alert("he");
                //swal("Uğurlu!", "", "");
                //setTimeout(function () { location.reload(); }, 5000);
                window.location.href = "UpdateNews"; }
         }
      })
    }
</script>